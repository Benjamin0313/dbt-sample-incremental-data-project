[project]
name = "jaffle-shop-datagen"
version = "0.1.0"
description = "源泉を宣言的ジェネレータ(datagen.yml + generate.py)で生成し、dbt-core で Snowflake に変換する jaffle_shop サンプル。"
requires-python = ">=3.11"
dependencies = [
    "dbt-snowflake==1.11.5",
    "faker==40.21.0",
    "pyyaml==6.0.3",
]
